@page "/"

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

<p>Search results:</p>

<ul>
    @if (VideosList != null)
    {
        @foreach (var video in VideosList)
        {
            <li>@video</li>
        }
    }
</ul>
